{
  "name": "football-pool-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tests for the small amount of client-side JavaScript the pool site ships. Node is a test-time dependency only — the site itself is built by Python and needs no bundler.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^26.0.0",
    "vitest": "^3.0.0"
  }
}
